import { Search } from '@material-ui/icons';
import { withStyles } from '@material-ui/core/styles';

const WhiteSearch = withStyles({
    root: {
        color:"white"
    },
  })(Search);

  export default WhiteSearch